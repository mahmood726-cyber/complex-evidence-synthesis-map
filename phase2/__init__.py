"""Phase 2 (P1) -- the multiverse layer.

Treats NMA analyst choices (heterogeneity model, tau2 estimator, CI method) as
forking-path axes, aggregates the spec-curve with the weighted-likelihood
combiner from spec-collapse-atlas (NEVER inverse-variance RE pooling -- C2), and
gates any treatment-hierarchy claim on POTH (C5).
"""
